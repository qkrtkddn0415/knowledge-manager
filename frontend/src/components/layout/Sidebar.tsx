import type { DocumentSummary } from '../../types/document'
import type { ViewName } from '../../app/routes'
import { Badge } from '../ui'

export default function Sidebar({ activeView, documents, onOpen, onHome, onLibrary, onAsk, onHistory }: { activeView: ViewName; documents: DocumentSummary[]; onOpen: (id: string) => void; onHome: () => void; onLibrary: () => void; onAsk: () => void; onHistory: () => void }) {
  return <aside className="sidebar"><div className="sidebar-section"><div className="sidebar-label">WORKSPACE</div><button className={'nav-item ' + (activeView === 'home' ? 'active' : '')} onClick={onHome} title="지식 유니버스"><span className="nav-icon" aria-hidden="true">✦</span><span>지식 유니버스</span></button><button className={'nav-item ' + (activeView === 'library' ? 'active' : '')} onClick={onLibrary} title="자료 라이브러리"><span className="nav-icon" aria-hidden="true">▤</span><span>자료 라이브러리</span></button><button className={'nav-item ' + (activeView === 'ask' ? 'active' : '')} onClick={onAsk} title="AI 대화"><span className="nav-icon" aria-hidden="true">◉</span><span>AI 대화</span></button><button className={'nav-item ' + (activeView === 'history' ? 'active' : '')} onClick={onHistory} title="검색 이력"><span className="nav-icon" aria-hidden="true">◷</span><span>검색 이력</span></button></div><div className="sidebar-section"><div className="sidebar-heading"><span>RECENT SOURCES</span><Badge tone="cyan">{documents.length}</Badge></div>{documents.slice(0, 5).map((document) => <button className="source-item" key={document.id} onClick={() => onOpen(document.id)}><span className="source-dot" /><span className="source-item-copy"><strong title={document.title}>{document.title}</strong><small title={document.source_name}>{document.source_name}</small></span></button>)}</div></aside>
}
