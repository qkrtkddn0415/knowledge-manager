export type AppSettings = {
  onboarding_completed: boolean
  openai_configured: boolean
  vector_store_configured: boolean
  data_directory_label: string
  export_available: boolean
  theme: 'dark'
  reduced_motion: boolean
}
